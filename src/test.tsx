import React from 'react';

// Put cursor at the end of FunctionComponent, View, or ViewProps
// and press Ctrl + space to see the suggestion, however, import won't work
const a: FunctionComponent

const Func: FunctionComponent<ViewProps> = () => {
    return (
        <View>
            hello world
        </View>
    )
}
